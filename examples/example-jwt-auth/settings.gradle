pluginManagement {
    repositories {
        maven { // The google mirror is less flaky than mavenCentral()
            url "https://repo1.maven.org/maven2/"
        }
        gradlePluginPortal()
    }
}
